<%# ダッシュボード: 従業員編集画面(フォームは _form.html.erb を共用) %>
<div class="dashboard-container">
  <%= render 'dashboard/shared/menu' %>

  <div class="subtitle">
    <h2>従業員編集</h2>
  </div>

  <%= render 'form', employee: @employee %>
</div>