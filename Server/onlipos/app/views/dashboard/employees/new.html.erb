<%# ダッシュボード: 従業員新規登録画面(フォームは _form.html.erb を共用) %>
<div class="dashboard-container">
  <%= render 'dashboard/shared/menu' %>

  <div class="subtitle">
    <h2>従業員登録</h2>
  </div>

  <%= render 'form', employee: @employee %>
</div>